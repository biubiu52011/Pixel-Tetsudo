# 5. 变更日志（2026-09-20）

> 本文件为 2026-09-20 变更归档；条目格式与正文组织沿用 archive/DEVELOPMENT-CHANGELOG-2026-09-18.md。

## 4.3.846（2026-09-20，大江戸線モバイル版站名ラベル可読性修复）

**问题**：用户上传手机竖屏截图反馈——都营大江户线（代码 `isSixShapedLoop` 六形环）手机版线路图站名标签重叠/错位/截断：光丘尾站名与环左列站名互相压字；"落合南長崎"首字被挤出左边界截成"合南長崎"；标签压在站点圆点上。根因（node 纯计算验证）：窄屏下左侧空间被光丘尾长站名占满，但环左列站也朝左，两组朝左站名 x 范围重叠；避让逻辑又把光丘尾站名整体推到画布左缘外（文字左缘 -22px），首字截断。

**修复**（仅移动端 `_isMobileView()` 分支生效，桌面端逻辑完全不动；全部在 `js/trains-page.js`）：
1. 环宽右边距 `marginRight` 移动端 `20*scale6 + 92` 改为 `+ 64`（收窄右区，给环内让出空间）。
2. 移动端 `loopRectW` 从"仅超宽时才缩小"改为"始终用剩余宽度撑满"（`if (_isMobileView()) { loopRectW = ... }`，桌面端保留 `else if (naturalW > ...)`）。
3. `_renderStationNode`：光丘尾站的左列上方避让循环用 `if (!isMobileView)` 包裹跳过（移动端环左列改朝右后无需避让）；主干环左列站（`_sixSide = "left"`）改为 `_sixSide = isMobileView ? "right" : "left"`——移动端环左列站名改朝右进环内。
4. 光丘尾站 clamp 宽度：移动端从 `junctionX - tx - 4`（向右空间，错误）改为 `tx - 4`（向左空间）。
5. `pages/trains.html` 中 `trains-page.js?v=4.3.620` bump 至 `?v=4.3.846`（强制浏览器拉新 JS）。

**验证**：
- `node --check js/trains-page.js` 语法通过（exit 0）；5 处修改标记逐一确认存在。
- 推送通道：此前在另一会话用 GitHub OAuth MCP `push_files` 手输 143KB 文件，三次误推（第一次只推 trains.html；第二次 content 仅文件开头约 80 行，把远程 trains-page.js 覆盖成残缺；第三次 content 写成占位符字符串），远程 main 一度损坏、线上列车页报错。本次改在 Windows 本地仓库（HEAD=17fff06 干净基线）重新应用 5 处修复，`git commit` + `git push --force origin main`（`+ d27e48f...5bcec4b forced update`）覆盖那 3 个坏 commit；推送后 `git show origin/main:js/trains-page.js` 确认远程文件完整（非残缺）且含全部修复标记。
- 移动端坐标经 node 纯计算：手机宽 344/360/380 CSS px 下，三组站名（光丘尾朝左 / 环左列朝右 / 环右列朝右）水平分离约 66px，无重叠、无越界、不穿右列竖线、不触发缩字。

**遗留**：手机端实际渲染效果由用户人工确认（项目打磨期规则：不系统截图确认，交付后由用户人工验收；诊断截图用完即删）。桌面端未改动，无回归。

## 4.3.847（2026-09-20，站间距切换为换乘 chip 高度自适应共同规则）

**用户指示**："站间距以换乘图标最高的加上一点空隙为每个线路的站间距，并且写入线路图共同规则"——直线线路站间距原按站数分档（≤20→72/62、21-30→66/58、31-50→60/54、>50→56/50，移动/桌面），与换乘 chip 无关；环线双列 _colPitch 本就按换乘 chip 高度自适应。本次将直线 sp 切换为同环线规则，并沉淀为共同规则（5.4.1.2.2）。

**修复**（js/trains-page.js）：
1. 新增公共函数 `_chipPitch(ids)`（computeRouteGeometry 内、transferMap 就绪后）：图标块高度 = 16(ICON) + 6(顶隙) + 行数×22(行高) + 12(底距)，行数 = ⌈min(换乘数,8)÷4⌉（每行最多 4 个、最多 2 行、溢出 "+n"）；站间距 = 全线各站块高最大值（保底 56px）。
2. 直线 sp 定义从按站数分档改为 `sp = _chipPitch(stations)`（移动/桌面统一）。
3. 环线双列 `_colPitch` / 六形环 `_colPitch6` 改为复用 `_chipPitch`（原函数体逐字节一致，单一权威，行为零变化）。
4. pages/trains.html 中 trains-page.js?v=4.3.846 bump 至 ?v=4.3.847。

**文档**（DEVELOPMENT.md）：5.4.1.2.2 标题与正文改写为「站间距与留白（换乘 chip 高度自适应）」共同规则（含公式、适用范围、环线引用）；5.4 直线画法旧分档条目删除改引用；5.4.2.1 标准环线表格补「列内垂直站距」行；5.4.2.2 环高描述补共同规则引用；目录锚点同步。

**验证**：
- node --check js/trains-page.js 通过；_chipPitch 四处（定义 / sp / _colPitch6 / _colPitch）确认就位。
- work/staging/verify_sp_chip.js 全量 166 线核算：44 线存在 2 行 chip 站 → sp=78（KeihinTohoku/Odawara/Keikyu/ChuoSobuLocal/Tojo/Noda 等换乘密集线 svgH 增大，桌面最大 +1200px）；122 线 sp=56（多数中小线 svgH 变小，桌面最多 −120px）。几何安全证明：每线 sp ≥ 最高 chip 底部 + 圆点半径 7（余量 19–24px），全部通过。换乘计数为 declared 上限（未计直通 through 过滤），实际 rows=2 的线可能少于 44 条，svgH 变化为上限值。
- 环线分支公式重构前后逐字节一致，环高行为零回归。

**遗留**：实际渲染效果由用户人工确认（项目打磨期规则：不系统截图确认，交付后由用户人工验收）。

## 4.3.848（2026-09-20，站间距公式对齐换乘图标 3 行上限）

**问题**：4.3.847 把站间距切换为 chip 高度自适应后，公式按 min(换乘数,8) 计算（最多 2 行，块高 78px），但渲染层 v4.3.613 已把换乘图标行数上限提到 3（RuntimeConfig.TRANSFER_MAX_ROWS=3，每行 4 个 = 最多 12 个图标，JR 大站东京/新宿换乘超 8 条；超出显示 "+n"）。3 行 chip 实际底部可达 ty+72，旧公式 sp=56/78 的线路会压到下一站圆点。

**修复**（js/trains-page.js `_chipPitch`）：行数计算 `min(换乘数,8)` → `min(换乘数,12)`（行数上限 3 = TRANSFER_MAX_ROWS，注释注明若行数上限再调需同步 min 值）。块高自动放大：1 行 56 / 2 行 78 / 3 行 100。pages/trains.html trains-page.js?v=4.3.847 bump 至 ?v=4.3.848。

**文档**（DEVELOPMENT.md）：5.4.1.2.2 公式与行数描述改 3 行上限；5.4.1.1.3 规格表「行数上限 2→3」（来源 4.3.613）；5.4.1.2 速览同步。

**验证**：
- node --check js/trains-page.js 通过。
- work/staging/verify_sp_chip.js 全量 166 线（3 行口径）：26 线 sp=100（declared 上限 >8 的换乘密集线：Yamanote/KeihinTohoku/Odawara/Keikyu/ChuoSobuLocal/Tojo 等，svgH 桌面最大 +2300px）；18 线 sp=78；122 线 sp=56。几何安全：每线 sp ≥ 3 行 chip 底部 + 圆点半径 7，余量 17–37px，全部通过。换乘计数为 declared 上限（未计直通 through 过滤/同系统去重），实际 3 行站可能少于 26 条，svgH 变化为上限值。
- 渲染行数上限 3 与公式 min(12) 严格一致（4×3=12）；未来行数上限若提为 4，改 TRANSFER_MAX_ROWS + 同步 min(16) 即可，规则参数化。

**遗留**：实际渲染效果由用户人工确认（项目打磨期规则：不系统截图确认，交付后由用户人工验收）。

## 4.3.849（2026-09-20，换乘图标行数上限 3→4（4×4=16），站间距公式同步 min(16)）

**用户裁定**："最多 4×4"——换乘图标行数上限由 3 提到 4（每行最多 4 个 = 最多 16 个图标，超出显示 "+n"）。

**修改**：
1. data/core/runtime-config.js：TRANSFER_MAX_ROWS 3→4（注释记录 v4.3.613 2→3、v4.3.849 3→4 沿革）；重跑 data/core/gen-config-bundle.js 重新生成 data-config-bundle.js（bundle 中 TRANSFER_MAX_ROWS=4 已确认）。
2. js/trains-page.js \_chipPitch：行数计算 min(换乘数,12) → min(换乘数,16)，注释同步（4×4=16，用户裁定；若行数上限再调需同步 min 值）。块高自动放大：1 行 56 / 2 行 78 / 3 行 100 / 4 行 122。
3. pages/trains.html trains-page.js?v=4.3.848 bump 至 ?v=4.3.849。

**文档**（DEVELOPMENT.md）：5.4.1.2.2 公式与行数描述改 4 行上限（min(换乘数,16)）；5.4.1.1.3 规格表「行数上限 3→4」（来源 4.3.849，沿革 972 记 2 / 4.3.613 记 3）；5.4.1.2 速览同步。

**验证**：
- node --check js/trains-page.js 通过；data-config-bundle.js 中 TRANSFER_MAX_ROWS=4 确认。
- work/staging/verify_sp_chip.js 全量 166 线（4 行口径）：当前数据最高 declared 换乘 11（<12），无站触发 4 行 → sp=122 的线 0 条；26 线 sp=100、18 线 sp=78、122 线 sp=56，与 4.3.848 实际图面一致（无新增变宽）。几何安全：全部 166 线通过，余量 17–37px；4 行几何（sp=122 ≥ 4 行 chip 底部 ty+92 + 圆点半径 7 = 99）成立，未来数据出现 4 行站时自动放大。
- 渲染行数上限 4 与公式 min(16) 严格一致（4×4=16）。

**遗留**：实际渲染效果由用户人工确认（项目打磨期规则：不系统截图确认，交付后由用户人工验收）。

## 4.3.850（2026-09-20，站间距标准重构为「行高 × 行数 + 一点空隙」）

**用户指示**："站间距以行高加上一点空隙为标准"——共同规则表述从「图标块高度 + 空隙」（16+6+行数×22+12）精化为「行高 × 行数 + 一点空隙」，数值不变。

**修改**（js/trains-page.js \_chipPitch）：
1. 定义 ROW_H=22（换乘图标行高，5.4.1.1.3）、SP_GAP=34（一点空隙 = ICON 16 + 顶隙 6 + 底距 12）；站间距 = 全线各站 (行数×ROW_H) 最大值 + SP_GAP。
2. 行数逻辑不变（⌈min(换乘数,16)÷4⌉，行数上限 4）；结果与 4.3.849 逐值一致：1 行 56 / 2 行 78 / 3 行 100 / 4 行 122。
3. pages/trains.html trains-page.js?v=4.3.849 bump 至 ?v=4.3.850。

**文档**（DEVELOPMENT.md）：5.4.1.2.2 改写为「换乘图标的高 + 一点空隙」标准（图标的高 = 行数 × 行高 22，空隙 34，可见空隙 17–23px 说明；句式经 4.3.847「图标块高度 + 空隙」、850「行高×行数 + 空隙」两轮表述统一回用户原话，数值全程不变）；5.4.1.2 速览同步。

**验证**：
- node --check js/trains-page.js 通过。
- work/staging/verify_sp_chip.js 重跑：结果与 4.3.849 完全一致（26 线 sp=100、18 线 sp=78、122 线 sp=56；166 线几何全通过，余量 17–37px）——纯语义重构，行为零变化。
- 可见空隙核算：chip 底部到下一站圆点顶缘 = sp − 7 − (14 + 行数×18 + (行数−1)×2) → 1 行 17px / 2 行 19px / 3 行 21px / 4 行 23px，均舒适。几何下限（可见空隙 0）对应 SP_GAP=17，当前 34 留有余量；如需调小空隙改 SP_GAP 一个常量即可。

**遗留**：实际渲染效果由用户人工确认（项目打磨期规则：不系统截图确认，交付后由用户人工验收）。
