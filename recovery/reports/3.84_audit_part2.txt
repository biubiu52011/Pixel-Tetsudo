
=== DISPLAY NAME CHAIN INCONSISTENCY ===

3. data-state.js: displayName fallback chain
   Code: (window.tLineName && window.tLineName(line)) || line.nameEn || line.name || lineId
   Problem: tLineName is NEVER DEFINED anywhere in the codebase
   Impact: Falls back to line.nameEn (internal ID like SeibuIkebukuro, ChuoRapid)
   Evidence: grep across all 20+ JS files confirms tLineName is never declared
   Expected: Should resolve nameJa first, then fall back gracefully
   Category: P2 (user sees internal IDs as names)

4. realtime-view.js vs data-state.js: DISPLAY NAME MISMATCH
   realtime-view.js getDisplayName: nameJa FIRST, then nameEn, then name
   data-state.js renderCard: tLineName (undefined) -> nameEn -> name -> lineId
   Impact: Same line shows DIFFERENT names in Realtime modal vs Trains card
   Category: CROSS_PAGE_INCONSISTENCY P2

=== TECHNICAL INFO LEAKAGE ===

5. TRAINS-DETAIL.JS: STATION IDs rendered directly in SVG
   Code: escapeHtml(stations[i]) for main line stations
   Impact: Raw station IDs like Kot手指, Shi nomi Cho appear in line map
   Expected: Should use RailwayDB.resolveStationName(stations[i], lang)
   Note: Branch stations correctly use tStation(bs), main line does not
   Category: TECHNICAL_LEAK P2

6. DATA-STATE.JS: OPERATOR NAMES rendered raw
   Code: escapeHtml(op) where op = line.operator (never assigned properly)
   Impact: Raw operator IDs like JR-East, Seibu, TokyoMetro shown as group titles
   Expected: Should use translated operator names (tOp or OP_NAMES map)
   Category: TECHNICAL_LEAK P2

=== INFORMATION ARCHITECTURE ===

7. HOME PAGE: Two module visual inconsistency
   Search module: div.module-title > h2 with class
   Tourism module: div.module-title > h2 (similar but check spacing/align)
   Both use pixel-card class but different internal structure
   Category: UX_INCONSISTENCY P3 (minor)

8. DETAIL PAGE: Technical fields shown to users
   detail.info_code -> shows line code (SI, JC, etc.)
   detail.info_operator -> shows raw operator name
   detail.info_type -> shows straight/loop (internal type)
   Question: Does a passenger need to know the line type?
   Category: INFORMATION_OVERLOAD P3

9. ROUTE SEARCH: Results lack key info
   Shows fare (search.fare) but not estimated travel time per segment
   Segment duration always shows N/A (DATA-BLOCKED per 3.74)
   Category: DATA-BLOCKED P3
